struct Vehicle {
    fn run() {
        self.engine = true;
        print("Vehicle is running", self.engine);
        fn sad() {
            print("sad", self);
        }   
        sad();
    }
}

struct Car {
    fn Create(name) {
        self.name = name;
        self.age = 1;
        print("Car", self.name, "created");
        return self;
    }

    fn run() {

    }
}

let car = Car().Create("BMW");
print(car.age);
car.age &&= 0;
print(car.age);